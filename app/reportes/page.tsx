'use client'; import {seedPrograma} from '@/lib/programa';
export default function Page(){const descargar=()=>{const h=['MAQUINA','PRODUCTO','ORDEN PRODUCCION','ESTATUS'];const body=seedPrograma.map(r=>[r.maquina,r.producto,r.op,r.estatus]);const csv=[h,...body].map(r=>r.map(v=>'"'+String(v??'').replace(/"/g,'""')+'"').join(',')).join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob(['\ufeff'+csv],{type:'text/csv;charset=utf-8'}));a.download='reporte_produccion.csv';a.click();URL.revokeObjectURL(a.href)};return <><div className="top"><div><div className="title">Reportes / Descargas</div><div className="muted">Diario, semanal, quincenal, mensual o rango personalizado</div></div></div><div className="card"><div className="form"><div className="field"><label>Desde</label><input type="date"/></div><div className="field"><label>Hasta</label><input type="date"/></div><div className="field"><label>Máquina</label><select><option>Todas</option></select></div><div className="field"><label>Turno</label><select><option>Ambos</option><option>Día</option><option>Noche</option></select></div><div className="field"><label>Estado</label><select><option>Todos</option><option>En proceso</option><option>Terminado</option><option>Incidencia</option></select></div><div className="wide actions"><button className="btn" onClick={descargar}>Descargar Excel/CSV</button><button className="btn secondary" disabled title="Se habilita al conectar el formato oficial">PDF oficial · siguiente etapa</button></div></div></div><section className="section"><div className="card"><b>El reporte por periodo se genera desde los registros de operación.</b><p className="muted">No requiere una segunda captura. Después lo vincularemos al formato oficial de producción.</p></div></section></>}
