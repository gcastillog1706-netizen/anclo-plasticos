import {seedPrograma} from '@/lib/programa';
export default function Page(){const n=seedPrograma.length,p=seedPrograma.filter(x=>x.estatus==='EN PROCESO').length,t=seedPrograma.filter(x=>x.estatus.includes('TERMIN')).length,inc=seedPrograma.filter(x=>x.estatus.includes('INCIDENCIA')||x.estatus==='SIN MATERIAL').length;return <><div className="top"><div><div className="title">KPIs / Informes</div><div className="muted">Indicadores alimentados por la operación</div></div></div><div className="grid"><div className="card"><div className="muted">Trabajos del programa</div><div className="kpi">{n}</div></div><div className="card"><div className="muted">En proceso</div><div className="kpi green">{p}</div></div><div className="card"><div className="muted">Terminados</div><div className="kpi">{t}</div></div><div className="card"><div className="muted">Bloqueados / incidencia</div><div className="kpi red">{inc}</div></div></div><div className="section two"><div className="card"><h2>KPIs que se habilitan con captura real</h2><p>Cumplimiento del programa · Pz/h real vs estándar · utilización · horas de paro · Pareto de causas · avance por OP · desviación Gantt · rendimiento de material · merma · purga · producción por turno y operador.</p></div><div className="card"><h2>OEE</h2><p>Se calculará cuando tengamos tiempo programado, paros, ciclo estándar, piezas totales y piezas buenas/rechazadas. No se mostrará un OEE ficticio.</p></div></div></>}
